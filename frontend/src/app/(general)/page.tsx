import { SearchBar } from '@/components';
import { PostsList } from '@/features';

const Home = () => {
  return (
    <>
      <SearchBar />
      <PostsList />
    </>
  );
};

export default Home;
